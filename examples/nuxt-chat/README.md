# Nuxt Chat Sample

This Nuxt app calls a server route (`/api/chat`) that uses `@lyre/ai-agents` in proxy mode.
Axis backend remains the single place that stores conversations/messages and tracks usage/cost.

## Run

```bash
cp .env.example .env
npm install
npm run dev
```
