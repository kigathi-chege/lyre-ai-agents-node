# SvelteKit Chat Sample

This sample uses a SvelteKit server endpoint to call `@lyre/ai-agents` in proxy mode.
Axis remains responsible for conversation/message persistence and usage metadata.

## Run

```bash
cp .env.example .env
npm install
npm run dev
```
