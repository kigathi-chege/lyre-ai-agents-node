# Express Chat Sample

This sample uses `@lyre/ai-agents` in `proxy` mode so Axis handles:

- Agent execution
- Conversation/message persistence
- Usage and cost tracking

## Run

```bash
cp .env.example .env
npm install
npm run dev
```

Open `http://localhost:3100`.
